---
version: 5
defaults:
  datadir: hiera
  data_hash: yaml_data
hierarchy:

  - name: "Nodes"
    path: "nodes/%{hostname}.yaml"

  - name: "Location"
    path: "%{location}.yaml"

  - name: "Hardware"
    path: "hardware/%{boardproductname}.yaml"

  - name: "Common"
    path: "common.yaml"
